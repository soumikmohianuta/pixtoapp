class TextValidator:
    
    def __init__(self, textWrapper, scalar, valid, log):
        self.textWrapper = textWrapper
        self.scalar = scalar
        self.valid = valid
        self.log = log
